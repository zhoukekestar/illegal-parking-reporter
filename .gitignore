# Node / Vite
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Tauri / Rust 产物
src-tauri/target/
src-tauri/gen/

# 本地模型 (体积大, 单独管理)
src-tauri/models/*.onnx
src-tauri/models/**/*.onnx
# segformer 子目录: optimum-cli 导出的全套 (config + onnx), 都不入库
src-tauri/models/segformer/
# pipeline DB / evidence 输出 (开发期生成)
src-tauri/.local/
# ultralytics 导出 ONNX 时下载的源权重, 不入库
*.pt
*.pth

# 临时文件
*.tmp
.cache/
